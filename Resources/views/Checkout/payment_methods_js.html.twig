<script type="text/javascript" src="{{ asset(frontendAsset('js/checkout-payment-widget.js')) }}"></script>
<script type="text/javascript">

    $(function(){
        var section = "payment_method";
        if (typeof accordion != 'undefined') {
            accordion.addSection(section);
        }
    });

    var paymentWidget = new CheckoutPaymentWidget({
        containerEl: $('div#section-payment_method'),
        buttonEl: $('div#section-payment_method a.next-button'),
        modalEl: $('div#confirm-order-modal'),
        postUrl: "{{ path('cart_checkout_update_section', {'section':'payment_method'}) }}",
        confirmOrderUrl: "{{ path('cart_checkout_confirm_order') }}",
        submitOrderUrl: "{{ path('cart_checkout_submit_order') }}"
    });
</script>
